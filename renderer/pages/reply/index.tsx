'use client';

import Empty from "../../components/misc/Empty";
import Wrapper from "../../components/providers/Wrapper";

interface ReplyPageProps {

}

const ReplyPage: React.FC<ReplyPageProps> = ({}) => {
  return (
    <Wrapper>
      <Empty />
      </Wrapper>
  );
};

export default ReplyPage;